<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Music</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<style>
  .profile {
    background-image: linear-gradient(to bottom, rgba(45, 255, 196, 0.2), rgba(45, 255, 196, 0.1), rgba(45, 255, 196, 0.05), rgba(45, 255, 196, 0.01), rgba(0, 0, 0, .1), rgba(0, 0, 0, .4), rgba(0, 0, 0, .72), rgba(0, 0, 0, .72));
  }

  .navlinks a.active::after {
    content: "";
    position: absolute;
    width: 50%;
    height: 1.4px;
    background: rgba(45, 255, 196, 0.7);
    left: 50%;
    transform: translate(-50%);
    bottom: -8px;
  }
</style>

<body class="w-full h-screen bg-[#101010]">
  <div class="w-full h-full px-14 py-12">
    <div class="relative w-full h-full text-white bg-[rgba(0, 0 ,0 , 0.72)] overflow-hidden">
      <div class="profile absolute w-full h-full pointer-events-none"></div>
      <form class=" px-16 py-12" method="post" action="/profile/uploader" enctype="multipart/form-data">
        <div class="flex gap-6">
          <div class="h-20 w-20 rounded-full hidden">
            <div class="bg-zinc-400/20">
              <label class="cursor-pointer " for="image">Change</label>
            </div>
            <input class="hidden" type="file" name="" id="image">
            <img src="" alt="">
          </div>
          <div class="h-32 w-32 rounded-full bg-zinc-400/10 flex items-center justify-center">
            <h1 class="text-4xl">A</h1>
          </div>
          <div class="py-5">
            <% if(user.isUploader) { %>
              <h4 class="text-zinc-400">Listener & uploader</h4>
              <% } else { %>
                <h4 class="text-zinc-400">Listener</h4>
                <% } %>
                  <h1 class="text-4xl font-semibold mt-1">Ashish Gupta</h1>
          </div>
        </div>
        <div class="px-2 py-16">
          <div class="flex navlinks relative items-center gap-20 tracking-[1.6px] font-semibold text-sm font-mono">
            <a href="" class="relative inline-block active">OVERVIEW</a>
            <a href="" class="relative inline-block">PUBLIC PLAYLISTS</a>
            <a href="" class="relative inline-block">FOLLOWING</a>
            <a href="" class="relative inline-block">FOLLOWERS</a>
          </div>
          <div class="w-3/4 py-16">
            <div class="flex flex-col gap-1">
              <h2 class="bg-emerald-300/5 px-4 py-2 shadow-xl flex items-center justify-between">
                <span>Username:</span><span>
                  <%= user.username %>
                </span>
              </h2>
              <h2 class="bg-emerald-300/5 px-4 py-2 shadow-xl flex items-center justify-between">
                <span>Email:</span><span>
                  <%= user.email %>
                </span>
              </h2>
              <h2 class="bg-emerald-300/5 px-4 py-2 shadow-xl flex items-center justify-between">
                <span>Playlists:</span><span>
                  <%= user.playlists.length %>
                </span>
              </h2>
              <h2 class="bg-emerald-300/5 px-4 py-2 shadow-xl flex items-center justify-between">
                <span>Favourites:</span><span>
                  <%= user.favourites.length %>
                </span>
              </h2>
              <h2 class="bg-emerald-300/5 px-4 py-2 shadow-xl flex items-center justify-between"><span>Created
                  At:</span><span>
                  <%= user.createdAt.toLocaleString() %>
                </span></h2>
              <h2 class="bg-emerald-300/5 px-4 py-2 shadow-xl flex items-center justify-between"><span>Updated
                  At:</span><span>
                  <%= user.updatedAt.toLocaleString() %>
                </span></h2>
              <h2 class="bg-emerald-300/5 px-4 py-2 shadow-xl flex items-center justify-between"><span>
                  <% if(user.isUploader) { %>
                    <h2>You can upload some songs.</h2>
                    <% } else { %>
                      <h2>If you want to become an uploader ?</h2>
                      <% } %>
                </span>
                <% if(user.isUploader) { %>
                  <a href="/music/upload" class="px-4 py-1 bg-emerald-800 text-white z-50">Upload Songs</a>
                  <% } else { %>
                    <input class="cursor-pointer px-4 py-1 bg-emerald-800 text-white z-50" type=" submit"
                      value="Click here">
                    <% } %>
              </h2>
            </div>
          </div>
        </div>
      </form>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const links = document.querySelectorAll('.navlinks a');

      links.forEach(link => {
        link.addEventListener('click', function (e) {
          e.preventDefault();
          links.forEach(l => l.classList.remove('active'));
          this.classList.add('active');
        });
      });
    });
  </script>

</body>

</html>